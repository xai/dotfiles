""" indentation
set autoindent " continue indentation to new line
set smartindent " add extra indent when it makes sense
set smarttab " <Tab> at the start of a line behaves as expected
set expandtab " <Tab> inserts spaces
set shiftwidth=4 " >>, << shift line by 4 spaces
set tabstop=4 " <Tab> appears as 4 spaces
set softtabstop=4 " <Tab> behaves as 4 spaces when editing

